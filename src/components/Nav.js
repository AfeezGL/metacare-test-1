import React from "react";

const Nav = () => {
  return (
    <nav
      role={"navigation"}
      aria-roledescription="Primary"
      aria-label="Primary"
    >
      <p>Gelion</p>
    </nav>
  );
};

export default Nav;
